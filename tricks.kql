//Lire prod depuis la dev
cluster('adxdataessprod.westeurope.kusto.windows.net').database('isight-data').contract()


//Récupérer le schéma d'un table
normalize_evh_telemetry_riva_metrics
| getschema
| project ColumnName, ColumnType
| extend CombinedColumn = strcat(ColumnName, ":", ColumnType)
| project CombinedColumn
| summarize CombinedColumn2 = strcat_array(make_list(CombinedColumn), ",")